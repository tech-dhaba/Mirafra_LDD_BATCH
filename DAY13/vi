/dts-v1/;
/plugin/;

/ {
    compatible = "brcm,bcm2835";

    fragment@0 {
        target = <&gpio>;
        _overlay_ {
            led_gpio = <&gpio 17 0>;  // GPIO 17 is used for the LED
        };
    };
};
